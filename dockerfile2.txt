FROM python:3.11-slim

WORKDIR /app

# Copy project files
COPY pyproject.toml README.md ./
COPY src ./src

# Install the package
RUN pip install --no-cache-dir .

# Run the CLI by default
ENTRYPOINT ["python", "-m", "cloudopt.job_runner"]

